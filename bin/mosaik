#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"

require "mosaik"

begin
  cli = MOSAIK::CLI
    .new(ARGV)

  cli
    .prepare

  cli
    .start
rescue MOSAIK::ExitError => e
  exit(e.message.to_i)
end
